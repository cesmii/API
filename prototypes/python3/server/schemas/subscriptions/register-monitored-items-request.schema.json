{
    "_comment": "RFC 4.2.3.2",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://github.com/cesmii/API/blob/prototypes/prototypes/python3/server/schemas/subscriptions/register-monitored-items-request.schema.json",
    "title": "Register Monitored Items Request",
    "description": "Request schema for registering monitored items to a subscription",
    "type": "object",
    "properties": {
        "elementIds": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Array of element IDs to monitor"
        },
        "includeMetadata": {
            "type": "boolean",
            "default": false,
            "description": "Whether to include metadata in subscription updates"
        },
        "maxDepth": {
            "type": "integer",
            "default": 0,
            "description": "Maximum depth for tree traversal (0 means no limit)"
        }
    },
    "required": ["elementIds"]
}
